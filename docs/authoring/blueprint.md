# Classic Blueprint Mode

Classic Unreal Blueprint remains a first-class Verse-atile authoring mode wherever the supported Unreal version provides Blueprint.

The goal is to preserve familiar Blueprint classes, functions, events, variables, components, interfaces, Animation Blueprints, Data Assets, and editor utilities.

Future compatibility work will favor non-destructive migration and adapters instead of forcing users to throw away existing Blueprint work.
